'use client';

import * as G from '@/components/globals.styles';

export default function Page() {
    return (
        <G.Root>
            <G.Centered>
                준비중
            </G.Centered>
        </G.Root>
    );
}